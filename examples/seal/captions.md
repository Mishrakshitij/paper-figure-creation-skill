# Suggested captions

**Teaser.** SEAL turns context into self-generated training material. The fictional example distinguishes a weight update from retaining a passage in the prompt. All rows of the paper's knowledge-incorporation table are shown across three update settings; SEAL leads in the single-passage setting but trails GPT-4.1 data under continued pretraining. The ARC panel includes the oracle comparator and uses the paper's eight curated evaluation tasks. Bold numbers identify each column’s best result. Compare methods within columns; the columns use different update settings. Values are reported percentages; uncertainty was not supplied in these tables. [Source: Tables 1–2](https://arxiv.org/html/2506.10943v2).

**Method.** Candidate edits independently adapt copies of the current model; downstream task performance determines which edits reinforce the generation policy. The green path denotes the outer training loop. The insets illustrate two self-edit representations. Candidate outcomes are schematic. The generic reward illustration retains improving edits; the knowledge experiment selects the best candidate. The outer reward loop is a training procedure, not a required inference step. [Source: method overview](https://jyopari.github.io/posts/seal), [Section 3 and Algorithm 1](https://arxiv.org/html/2506.10943v2).

**ARC measurement unit.** The reported SEAL success rate counts self-edit adaptations that produce a correct held-out output. Five edits are independently applied for each of eight curated evaluation tasks. The task selection uses solvability under the oracle configuration. [Source: Section 4.1](https://arxiv.org/html/2506.10943v2#S4.SS1).
