**LoRA trades a smaller trainable update for strong task accuracy.** Validation results for all eight GPT-3 settings in LoRA v2, Table 4, on WikiSQL (logical-form accuracy) and MNLI-matched (classification accuracy). The horizontal axis is trainable parameters in millions on a logarithmic scale; it is not measured latency or training compute. Distinct settings retain their exact coordinates. Leader labels separate the nearby 37.7M LoRA and 40.1M AdapterH MNLI results without jitter. The smaller LoRA setting is below full fine-tuning on WikiSQL, and the two LoRA settings reverse order across tasks. Table 4 does not provide entry-specific uncertainty, so no error bars or significance claims are added.
